# IBeam

A placeholder folder to contain the env.list file. Before running the docker commands, create a new file named env.list in the local folder with the following contents.

```
IBEAM_ACCOUNT={REPLACE_WITH_IBKR_USERNAME}
IBEAM_PASSWORD={REPLACE_WITH_IBKR_PASSWORD}
```